---
answer: B
refs:
  - c1-s02 " " で囲んだ部分は空白を含めて1つの引数になる
  - c6-s02 NumberFormatException は数値に変換できない文字列を parseInt したときに出る
---
次のプログラムを、あとに示すコマンドで実行したときの結果として、正しいものを選びなさい。

```java title=Calc.java
public class Calc {
    public static void main(String[] args) {
        int sum = 0;
        for (String a : args) {
            sum += Integer.parseInt(a);
            System.out.print(sum + " ");
        }
    }
}
```

```console
> java Calc 3 "4 5" 6
```

- A. 「3 7 12 18」と表示される
- B. 「3 」と表示された後、NumberFormatException がスローされる
- C. 「3 48 54」と表示される
- D. 何も表示されずに NumberFormatException がスローされる
- E. コンパイルエラーが発生する

## 解説
ダブルクォートで囲んだ `"4 5"` は、空白を含めて **1 つの引数**になります。args は `{"3", "4 5", "6"}` の 3 要素です（A のように 4 つに分かれることはありません）。

1 周目は `parseInt("3")` で sum が 3 になり「3 」を表示します。2 周目の `parseInt("4 5")` は、空白を含むので数値に変換できず、**NumberFormatException** がスローされます。main で捕まえていないので、プログラムはここで終わります。

表示してから例外が起きるまでの順番を追うのがポイントです。「3 」は例外より前に出力済みなので、D は誤りです。

## 検証
@case 正解 B
@stem
@javac
@run Calc 3 "4 5" 6
@output
3 
@end
@exception NumberFormatException
